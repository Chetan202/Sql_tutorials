
-- 1 ) @@datefirst
SELECT @@DATEFIRST AS T

-- 2 ) @@dbTS
SELECT @@DBTS AS T

-- 3 ) LANGID
SELECT @@LANGID

-- 4 ) LANGUAGE
SELECT @@LANGUAGE

-- 5 ) LOCK_TIMEOUT
SELECT @@LOCK_TIMEOUT

-- 6 ) MAX_CONNCECTIONS
SELECT @@MAX_CONNECTIONS

-- 7 ) MAX_PRECISION
SELECT @@MAX_PRECISION

-- 8 ) NESTLEVEL
SELECT @@NESTLEVEL

-- 9 ) OPTIONS
SELECT @@OPTIONS

-- 10 ) REMSERVER
SELECT @@REMSERVER

-- 11 ) SERVERNAME
SELECT @@SERVERNAME

-- 12 ) SERVICENAME
SELECT @@SERVICENAME

-- 13 ) SPID
SELECT @@SPID

-- 14 ) TEXTSIZE 
SELECT @@TEXTSIZE

-- 15 ) VERSION
SELECT @@VERSION

ALTER DATABASE Demo1
SET SINGLE_USER WITH ROLLBACK IMMEDIATE

exec sp_renamedb 'DEMO2','DB2'

ALTER DATABASE DB2
SET MULTI_USER WITH ROLLBACK IMMEDIATE

